#include <doctest/doctest.h>
#include "lib2/lib2.h"

using namespace TestProject;

TEST_SUITE(TP_TEST_SUITE) {
  TEST_CASE("call_lib2") {
    CHECK(true);
  }

  TEST_CASE("other_lib2") {
    CHECK(true);
  }
}
